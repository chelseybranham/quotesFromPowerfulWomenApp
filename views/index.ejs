<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quotes from Powerful Women</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
    <h1 data-position="header">Quotes from Powerful Women</h1>
    <section data-position="add-quote">
        <h2>Add quote</h2>

    <form action='/quotes' method="POST">
        <input type="text" placeholder="name" name="name">
        <input type="text" placeholder="quote" name="quote">
        <button type="submit">Submit</button>
      </form></section>

   
    <section data-position="update-quote">
        <div>
            <h2>Replace a Quote</h2>
            <p>
            Replace a quote with a new quote 
            </p>
            <button id="update-button">Replace quote</button>
            </div>

        </section>
            <div>
    <section data-position="delete-quote">
        <h2>Remove a Quote</h2>
                <p>
                Delete one quote. Does nothing if there are no
                more quotes.
                </p>
                <button id="delete-button">Delete quote</button>
                </div>
                <div id="message"></div>
            </section>
            <section data-position="quotes">
                <h2>Quotes Library</h2>
                <ul class="quotes">
                  <!-- Loop through quotes -->
                  <% for(var i = 0; i < quotes.length; i++) {%>
                  <li class="quote">
                  <!-- Output name from the iterated quote object -->
                  <span><%= quotes[i].name %></span>:
                  <!-- Output quote from the iterated quote object -->
                  <span><%= quotes[i].quote %></span>
                  </li>
                  <% } %>
                  </ul>
              </section>
            <script src='main.js'></script>
</body>
</html>